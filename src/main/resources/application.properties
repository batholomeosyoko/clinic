spring.datasource.url=jdbc:postgresql://localhost:${DB_PORT:5433}/${DB_NAME:hcms}
spring.datasource.username=${DB_USER:hcms_user}
spring.datasource.password=${DB_PASSWORD:hcms_pass}
jwt.secret=${JWT_SECRET:dev-only-fallback-do-not-use-in-production}
